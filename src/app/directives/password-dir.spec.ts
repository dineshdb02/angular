import { PasswordDir } from './password-dir';

describe('PasswordDir', () => {
  it('should create an instance', () => {
    const directive = new PasswordDir();
    expect(directive).toBeTruthy();
  });
});
